
# Second window
def addGameLength(arg):
    # gameLength = gameLengthConfirm
    gameLength = arg
    print(str(gameLength) + " hours")

def addSessionCount(arg):
    # NEED TO DISABLE BUTTON UNTIL GAME LENGTH IS CONFIRMED
    # gameLength = gameLengthConfirm
    sessionCount = arg
    print(str(sessionCount) + " sessions over " + str(arg) + " hours")